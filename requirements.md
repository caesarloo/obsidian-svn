# Obsidian SVN 版本管理插件 - 需求文档 (v1.0)

## 1. 背景与目标
- 在 Obsidian 内提供 SVN 版本管理能力，管理本地笔记库。
- 降低对命令行的依赖，同时保持标准 SVN 工作流。

## 2. 范围
### 包含范围 (MVP)
- 基于已有工作副本（不含检出）。
- 更新工作副本。
- 刷新工作副本状态。
- 查看工作副本状态。
- 添加 / 删除 / 还原文件。
- 提交变更并填写备注。
- 生成提交备注摘要（Copilot）。
- 基础冲突提示与指引。

### 不包含范围 (本期)
- 检出仓库到本地目录。
- 日志与历史。
- 分支/标签管理。
- 高级权限流程。
- 大文件锁定策略的深度支持。
- 内置图形化合并工具。

## 3. 用户与场景
- 个人知识库用户，基于已有工作副本进行版本管理。
- 团队协作用户，使用共享 SVN 工作副本协作。
- 轻度技术用户，更倾向使用界面操作。

## 4. 功能需求
### 4.1 仓库配置
- 绑定已有本地库到 SVN 仓库。
- 配置仓库 URL 与凭据。
- 支持可选的安全凭据存储（平台支持时）。
- 校验本地路径为有效 SVN 工作副本。

### 4.2 状态刷新
- 手动刷新状态视图（仅重新执行状态扫描，不触发更新）。
- 刷新失败提示并保留上次状态。

### 4.3 状态视图
- 显示文件状态：新增、修改、删除、冲突、未跟踪。
- 文件树单层展示变动文件与其文件夹。
- 文件夹显示完整路径，文件仅显示文件名。
- 支持复选框多选文件/文件夹；文件夹勾选包含其子项。
- 文件夹支持折叠/展开（折叠为 "-"，展开为 "+"）。

### 4.4 文件操作
- 添加文件（svn add）。
- 删除文件（svn delete）。
- 还原文件（svn revert）。
- 标记冲突已解决（svn resolve）。
- 更新或提交后提供冲突处理提示。

### 4.5 更新与提交
- 仅支持更新整个库（当前仓库所在文件夹），不支持子路径更新。
- 提交支持选中或全部变更，默认全部选中。
- 支持一键生成提交备注摘要（由 Copilot 生成）。
- 更新或提交完成后自动刷新状态视图。

## 5. 非功能需求
- 性能：<= 1000 文件的状态扫描不超过 3 秒。
- 稳定性：失败时给出清晰错误提示。
- 安全：可选凭据加密存储（平台支持时）。
- 兼容性：Windows/macOS/Linux；Obsidian 1.x 插件 API。

## 6. UI 与交互
- 侧边栏面板：仅快速操作图标（更新 / 刷新 / 生成摘要 / 提交 / 设置）。
- 提交模块内联位于变动文件列表上方，包含备注输入框。
- 支持通过快速操作触发提交。
- 变动文件区以可折叠文件夹与文件列表展示，无父目录层级。
- 命令面板：核心操作入口。
- 冲突面板仅在存在冲突时显示。
- 弹窗：仅用于仓库配置。

## 7. 错误处理
- 认证失败。
- 未找到 SVN 可执行文件。
- 网络错误。
- 合并冲突。
- 工作副本无效或未初始化。
- 更新/提交失败。

## 8. 依赖与假设
- 已安装 SVN CLI 且在 PATH 中可用。
- 插件通过调用 svn 命令实现功能。

## 9. 交付物
- 插件源代码。
- 用户使用说明。
- 安装与配置说明。

## 10. 里程碑 (建议)
- M1：基础 CLI 集成。
- M2：状态/更新/提交。
- M3：冲突流程与 UI 打磨。
