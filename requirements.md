# Vault SVN 版本管理插件 - 需求文档 (v1.1)

## 1. 背景与目标
- 在 Obsidian 内提供 SVN 版本管理能力，管理本地笔记库。
- 降低对命令行的依赖，同时保持标准 SVN 工作流。

## 2. 范围
### 包含范围 (MVP)
- 基于已有工作副本（不含检出）。
- 更新工作副本。
- 刷新工作副本状态。
- 查看工作副本状态。
- 添加 / 删除 / 还原文件。
- 提交变更并填写备注。
- 生成提交备注摘要（Copilot）。
- 基础冲突提示与指引。

### 不包含范围 (本期)
- 检出仓库到本地目录。
- 日志与历史。
- 分支/标签管理。
- 高级权限流程。
- 大文件锁定策略的深度支持。
- 内置图形化合并工具。

## 3. 用户与场景
- 个人知识库用户，基于已有工作副本进行版本管理。
- 团队协作用户，使用共享 SVN 工作副本协作。
- 轻度技术用户，更倾向使用界面操作。

## 4. 功能需求
### 4.1 仓库配置
- 配置 SVN 认证凭据（用户名、密码）。
- 支持可选的安全凭据存储（使用 Obsidian 提供的安全存储机制）。
- 工作副本默认使用当前 Obsidian 库根目录。

### 4.2 状态刷新
- 手动刷新状态视图（仅重新执行状态扫描，不触发更新）。
- 刷新失败提示并保留上次状态。

### 4.3 状态视图
- 显示文件状态：新增、修改、删除、冲突、未跟踪。
- 文件树单层展示变动文件与其文件夹。
- 文件夹显示完整路径，文件仅显示文件名。
- 文件/文件夹右侧提供“暂存/还原”图标动作：
	- “暂存”仅作为提交选择，不对应 SVN 命令。
	- “还原”对应还原（svn revert）。
- 已暂存文件显示“已暂存”标签。
- 已暂存文件的“暂存”按钮切换为“取消暂存”（减号图标），点击后移除“已暂存”标签。
- 文件夹支持折叠/展开（折叠为 "-"，展开为 "+"）。

### 4.4 文件操作
- 添加文件（svn add）。
- 删除文件（svn delete）。
- 还原文件（svn revert）。
- 文件夹“还原”采用递归还原其子项，执行前需二次确认。
- 标记冲突已解决（svn resolve）。
- 更新或提交后提供冲突处理提示。
- 点击文件可以查看文件差异（svn diff）：
  - 采用内联对比为主，侧边对比为辅的展示方式
  - 支持与最新版本的差异对比
  - 对于大文件，采用异步加载和分页显示

### 4.5 更新反馈
- 更新后显示本次更新的文件列表：
  - 使用通知提示更新完成
  - 在面板中展示详细的更新文件列表
  - 支持点击文件查看详细信息
  - 添加更新统计信息（文件数量、大小等）

### 4.6 更新与提交
- 仅支持更新整个库（当前仓库所在文件夹），不支持子路径更新。
- 提交基于“暂存”集合；未暂存时提示先暂存。
- 支持一键生成提交备注摘要（由 Copilot 生成）。
- Copilot 生成摘要仅填充提交日志文本框，不自动触发提交。
- Copilot 生成摘要失败时仅提示，不阻断手动提交。
- 更新或提交完成后自动刷新状态视图。

## 5. 非功能需求
- **性能**：
  - <= 1000 文件的状态扫描不超过 3 秒（常规开发机、热缓存场景）。
  - 大文件处理策略：对于大文件，采用异步处理和进度提示。
  - 网络环境影响：添加网络状态检测和相应的错误处理。
- **稳定性**：
  - 失败时给出清晰错误提示。
  - 完善错误处理机制：详细分类错误类型和处理流程。
  - 添加日志记录机制：支持不同级别的日志，便于问题排查。
- **安全**：
  - 可选凭据加密存储（使用 Obsidian 提供的安全存储机制）。
  - 添加敏感操作的权限控制机制：如敏感操作的确认机制。
- **兼容性**：Windows/macOS/Linux；Obsidian 1.x 插件 API。

## 6. UI 与交互
- 侧边栏面板：仅快速操作图标（更新 / 刷新 / 生成摘要 / 提交 / 设置）。
- 提交模块内联位于变动文件列表上方，包含备注输入框。
- 支持通过快速操作触发提交。
- 变动文件区以可折叠文件夹与文件列表展示，无父目录层级。
- 变动文件行右侧展示“暂存/还原”图标。
- 命令面板：核心操作入口。
- 冲突面板仅在存在冲突时显示。
- 弹窗：仅用于仓库配置（用户名、密码）。

## 7. 错误处理
- 认证失败。
- 未找到 SVN 可执行文件。
- 网络错误。
- 合并冲突。
- 工作副本无效或未初始化。
- 更新/提交失败。
- Copilot 摘要生成失败（不阻断手动提交）。

## 8. 依赖与假设
- 已安装 SVN CLI 且在 PATH 中可用。
- 插件通过调用 svn 命令实现功能。

## 9. 交付物
- 插件源代码。
- 用户使用说明。
- 安装与配置说明。

## 10. 里程碑 (建议)
- M1：基础 CLI 集成。
- M2：状态/更新/提交（含暂存/取消暂存）。
- M3：冲突流程、仓库凭据配置与 UI 打磨。
- M4：文件差异查看和更新反馈功能。
- M5：分支/标签管理（未来规划）。

## 11. 术语定义
- **工作副本**：SVN 中本地的代码或文件副本，用户可以在其中进行修改。
- **暂存**：在提交前标记需要包含在提交中的文件。
- **还原**：撤销对文件的修改，恢复到之前的版本。
- **冲突**：当两个用户对同一文件的同一部分进行修改时产生的冲突。
- **更新**：从 SVN 服务器获取最新版本的文件。
- **提交**：将本地修改的文件保存到 SVN 服务器。
- **差异**：文件的不同版本之间的变化。

## 12. 验收标准

### 12.1 功能需求验收标准

#### 12.1.1 仓库配置
- 能够成功配置 SVN 认证凭据。
- 凭据能够安全存储。
- 能够正确设置工作副本路径。

#### 12.1.2 状态刷新
- 手动刷新状态视图能够正确显示最新状态。
- 刷新失败时能够给出清晰的错误提示。
- 刷新失败后能够保留上次状态。

#### 12.1.3 状态视图
- 能够正确显示文件的各种状态（新增、修改、删除、冲突、未跟踪）。
- 文件树能够正确展示变动文件与其文件夹。
- 能够正确显示“暂存/还原”图标动作。
- 已暂存文件能够显示“已暂存”标签。
- 文件夹能够正确折叠/展开。

#### 12.1.4 文件操作
- 能够成功添加文件。
- 能够成功删除文件。
- 能够成功还原文件。
- 文件夹“还原”能够递归还原其子项，并在执行前进行二次确认。
- 能够成功标记冲突已解决。
- 点击文件能够查看文件差异，采用内联对比为主，侧边对比为辅的展示方式。
- 对于大文件，能够采用异步加载和分页显示。

#### 12.1.5 更新反馈
- 更新完成后能够显示通知提示。
- 能够在面板中展示详细的更新文件列表。
- 能够点击文件查看详细信息。
- 能够显示更新统计信息（文件数量、大小等）。

#### 12.1.6 更新与提交
- 能够成功更新整个库。
- 提交时能够基于“暂存”集合。
- 未暂存时能够提示先暂存。
- 能够一键生成提交备注摘要。
- 更新或提交完成后能够自动刷新状态视图。

### 12.2 非功能需求验收标准

#### 12.2.1 性能
- <= 1000 文件的状态扫描不超过 3 秒。
- 大文件处理能够采用异步处理和进度提示。
- 能够检测网络状态并进行相应的错误处理。

#### 12.2.2 稳定性
- 失败时能够给出清晰错误提示。
- 能够详细分类错误类型和处理流程。
- 能够记录不同级别的日志，便于问题排查。

#### 12.2.3 安全
- 凭据能够安全存储。
- 敏感操作能够进行权限控制和确认。

#### 12.2.4 兼容性
- 能够在 Windows/macOS/Linux 上正常运行。
- 能够在 Obsidian 1.x 及以上版本上正常运行。
