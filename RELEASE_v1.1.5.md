# Vault SVN 插件 v1.1.5 发布说明

## 发布日期
2026-03-04

## 主要更改

### 新增
1. **新增侧边栏标签图标**：为 Vault SVN 面板提供自定义图标，并在标签页位置展示。
2. **新增差异对比模式标识**：差异视图标题会显示“本地对比”或“历史对比”，便于区分来源。

### 修复
1. **修复自动刷新间隔不生效问题**：按设置间隔启动/重建自动刷新定时器，关闭视图时自动清理。
2. **修复更新后文件差异显示为“无差异”问题**：更新反馈列表点击文件时改为与上一版本对比（含回退策略）。

### 改进
1. **品牌文案统一**：用户可见文案统一为 Vault SVN。
2. **设置项精简**：移除用户名、密码、持久化保存密码配置，保留并强化 svn 可执行文件配置。

## 版本更新
- 更新版本号到 `1.1.5`

## Release 附件

本次发布的附件包含以下文件：
- `dist/main.js` - 插件主程序
- `dist/manifest.json` - 插件清单
- `dist/styles.css` - 插件样式
