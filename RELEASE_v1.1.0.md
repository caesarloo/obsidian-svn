# Obsidian SVN 插件 v1.1.0 发布说明

## 发布日期
2026-03-02

## 主要更改

### 差异查看修复
1. **修复点击变动文件后差异视图不显示的问题**：
   - 优化 `svn diff` 输出解析逻辑，仅在 hunk（`@@`）区段解析差异行
   - 忽略 `Index:`、`===`、`---`、`+++` 等元信息行，避免误解析
   - 增加归一化回退机制：当归一化误删全部变更时，回退到原始差异行

### 中文编码修复
2. **修复差异内容中文乱码问题**：
   - `svn diff` 改为原始 UTF-8 解码路径，避免进入通用自动猜测导致的误判
   - 新增按行乱码修复策略（针对 UTF-8 被误按 latin1 解码场景）
   - 仅在“修复后中文字符更合理且不引入更多替换字符”时应用替换，降低误伤

### 可靠性改进
3. **增强差异解析稳定性**：
   - 多文件 diff 输出下更稳健地识别文件边界
   - 保持现有空白/Markdown 分隔线降噪逻辑，同时避免有效变更被吞掉

## 版本更新
- 更新版本号到 `v1.1.0`

## Release 附件

本次发布的附件包含以下文件：
- `dist/main.js` - 插件主程序
- `dist/manifest.json` - 插件清单
- `dist/styles.css` - 插件样式

## 安装方法

1. 下载发布附件
2. 将附件中的文件复制到 Obsidian 插件目录 `obsidian-svn` 文件夹
3. 重启 Obsidian 或在社区插件页面重新加载插件
4. 在 Obsidian 设置中启用 "Obsidian SVN" 插件

## 兼容性

- 仅支持桌面端 Obsidian
- 需要本机可用的 `svn` 命令行工具

## 反馈与支持

如果使用过程中仍遇到乱码或差异显示异常，请在仓库中提交 issue，并附上控制台日志片段（建议包含 `stdoutSample`）。
