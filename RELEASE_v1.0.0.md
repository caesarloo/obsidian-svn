# Vault SVN v1.0.0

首个稳定版本发布，提供 Obsidian 桌面端 SVN 工作副本管理能力。

## 功能亮点

- 支持工作副本更新（`svn update`）与状态刷新（`svn status`）
- 支持按目录展示变更树：新增、修改、删除、冲突、未跟踪、缺失
- 支持文件与文件夹级暂存/取消暂存
- 支持文件还原与文件夹递归还原
- 支持仅提交“已暂存”变更集合
- 支持提交摘要自动生成（失败不阻断手动提交）
- 支持冲突提示与“标记已解决”
- 支持仓库配置弹窗（路径、可执行文件、用户名、密码）

## 附件

请下载并安装以下文件：

- `main.js`
- `manifest.json`
- `styles.css`

## 安装方式

将上述三个文件放入 Obsidian 插件目录：

`<Vault>/.obsidian/plugins/obsidian-svn/`

重启 Obsidian 或在社区插件中重新加载本插件。

## 说明

- 仅支持桌面端 Obsidian
- 依赖系统已安装可执行的 `svn` 命令行工具
